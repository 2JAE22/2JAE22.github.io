name: Deploy Jekyll site to Pages

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.7'

      - name: Install dependencies
        run: |
          gem install bundler
          bundle install --path vendor/bundle

      - name: Build Jekyll site
        run: |
          bundle exec jekyll build --trace || (echo "Jekyll build failed!" && exit 1)

      - name: Debug Check _site folder
        run: |
          echo "Checking _site folder..."
          ls -l _site || (echo "_site folder not found!" && exit 1)

      - name: Remove symlinks
        run: |
          find _site -type l -delete

      - name: Set permissions
        run: |
          chmod -R a+rX _site

      - name: Upload artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
      run: echo ${{ steps.deployment.outputs }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      - name: Debug Check outputs
        run: echo "${{ toJSON(steps.deployment.outputs) }}" 